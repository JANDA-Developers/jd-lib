import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import Doc, { DocHeader, DocSection } from '../src/components/doc/Doc';
import '../src/scss/all.scss';
import { IDiv } from '../src/types/interface';
import JDcontainer from '../src/components/container/Container';
import { WindowSize } from '../src/types/enum';

# Doc

## Doc 는 css 규격을 유한 컴포넌트 입니다. **MDX**!

DOC prop **Extends Div**

<Props of={Doc} />

Doc Header Props **Extends Div**

<Props of={DocHeader} />
